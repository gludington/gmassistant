# Local dev: uses a file-based SQLite database (no auth token needed)
DATABASE_URL=file:./gmassisstant.db

# Production (Turso): set these when deploying
# DATABASE_URL=libsql://your-db.turso.io
# DATABASE_AUTH_TOKEN=your-token-here

PORT=3000
